#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main()
{
    ll t = 1;
    //    cin >> t;
    while (t--)
    {

        ll a, b;
        cin >> a >> b;
        cout << (a - b) - 2 << endl;
    }
    return 0;
}